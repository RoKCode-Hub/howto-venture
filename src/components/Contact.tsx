const Contact = () => {
  return (
    <section id="contact" className="py-1 bg-white">
      {/* Empty contact section - matching original which doesn't have visible contact section */}
    </section>
  );
};

export default Contact;
